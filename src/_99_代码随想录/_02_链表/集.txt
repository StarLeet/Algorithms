反转链表：
- 迭代
- 尾递归(可以根据迭代写出来) 原理是：传前置指针prev + 虚拟头节点

设计链表：
- 有点难，滑铁卢了

链表相交：
- 核心：找到一个节点相等即可
- 方法：使两个链表遍历长度一样(不需要拼接)

环形链表：
- 快慢指针判断有无环时,不建议提前判出,而是让fast、slow成对执行后再判断

递归三部曲：
1. 找整个递归的终止条件：递归应该在什么时候结束？
2. 找返回值：应该给上一级返回什么信息？
3. 本级递归应该做什么：在这一级递归中，应该完成什么任务？